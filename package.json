{
  "name": "katty-hozavsky-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "media:manifest": "node scripts/generate-media-manifest.mjs",
    "dev": "node scripts/generate-media-manifest.mjs && vite",
    "build": "node scripts/generate-media-manifest.mjs && tsc -b && vite build",
    "preview": "vite preview",
    "lint": "node scripts/generate-media-manifest.mjs && tsc -b --pretty false"
  },
  "dependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
